#include <iostream>
using namespace std;


 void Test_execute(string file_name){
        int nr_questions;
        string line;
        system('cls');
        ifstream file(file_name);
        if (!file.is_open()) { 
        cerr << "Error opening the file!" << endl; 
        exit(1);
    }
        cin>>nr_questions;
        question_type_answer[nr_questions][1];
        for(int i=0;i<nr_questions;++i){
            
        }
    }